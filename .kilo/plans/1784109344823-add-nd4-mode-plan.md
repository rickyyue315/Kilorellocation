# Plan: Add ND4 Mode — ND限同OM轉貨(補0及有銷售記錄)

## Overview

Add a new mode **ND4** based on ND3, but restrict receiving stores to only those with sales records (Last Month Sold Qty + MTD Sold Qty > 0).

- ND3 destination filter: `Net Stock == 0` only
- ND4 destination filter: `Net Stock == 0` **AND** `Last Month Sold Qty + MTD Sold Qty > 0`

All other logic (source rules, matching, pairing, receive site limit) is identical to ND3.

---

## Files to Modify (in order)

### 1. `config.py`
- Bump `VERSION` from `"v2.28.0"` to `"v2.29.0"`
- No new constants needed (ND3_KEEP_STOCK reused)

### 2. `models/mode_registry.py`
Add ND4 ModeDef **after ND3** (line 134):
```python
ModeDef("ND4", "ND限同OM轉貨(補0及有銷售記錄)",
        "ND4 同OM補0 + 僅限有銷售記錄Shop接收,轉出保留3件",
        attr_name="mode_nd4",
        families=frozenset({'nd_transfer'}), source_filter=True,
        strategy_key='nd_mode', receive_site_limit=True,
        source_method='_sources_nd4_mode', dest_method='_dests_nd4_mode'),
```

### 3. `strategies/nd_mode.py`
#### 3a. Add `identify_sources_nd4_mode()` — identical to ND3, but label `ND4轉出(保留3件)`:
```python
def identify_sources_nd4_mode(group_df: pd.DataFrame) -> List[Dict]:
    sources: List[Dict] = []
    nd_stores = group_df[group_df['RP Type'] == 'ND']
    max_nd_sold = compute_max_protected_sold(nd_stores)
    for _, row in nd_stores.iterrows():
        net_stock = int(row['SaSa Net Stock'])
        if net_stock <= ND3_KEEP_STOCK:
            continue
        effective_sold = int(row['Effective Sold Qty'])
        if effective_sold >= max_nd_sold:
            continue
        transferable_qty = net_stock - ND3_KEEP_STOCK
        last_month_sold = int(row['Last Month Sold Qty'])
        mtd_sold = int(row['MTD Sold Qty'])
        total_sales = last_month_sold + mtd_sold
        sources.append(make_source(row, transferable_qty, 1, 'ND4轉出(保留3件)',
                                    total_sales_sort=total_sales))
    sources.sort(key=lambda x: x.get('total_sales_sort', 0))
    return sources
```

#### 3b. Add `identify_destinations_nd4_mode()` — ND3 logic + sales filter:
```python
def identify_destinations_nd4_mode(group_df: pd.DataFrame) -> List[Dict]:
    destinations: List[Dict] = []
    nd_stores = group_df[group_df['RP Type'] == 'ND']
    for _, row in nd_stores.iterrows():
        net_stock = int(row['SaSa Net Stock'])
        if net_stock > 0:
            continue
        # ND4: only shops with sales records
        last_month_sold = int(row['Last Month Sold Qty'])
        mtd_sold = int(row['MTD Sold Qty'])
        total_sales = last_month_sold + mtd_sold
        if total_sales <= 0:
            continue
        safety_stock = int(row['Safety Stock']) if pd.notna(row.get('Safety Stock', 0)) else 0
        total_available = net_stock + int(row['Pending Received'])
        target_qty = max(int(safety_stock * F_TARGET_MULTIPLIER), F_TARGET_FLOOR)
        needed_qty = target_qty - total_available
        if needed_qty <= 0:
            continue
        destinations.append(make_dest(row, needed_qty, 1, 'ND4補0接收(有銷量)', target_qty,
                                       max_receive_qty=target_qty,
                                       total_sales=total_sales))
    destinations.sort(key=lambda x: (x['priority'], -x.get('total_sales', 0)))
    return destinations
```

#### 3c. Update `NDModeStrategy`:
- `identify_sources()`: add route for `"ND限同OM轉貨(補0及有銷售記錄)"` → `identify_sources_nd4_mode()`
- `identify_destinations()`: add route for `"ND限同OM轉貨(補0及有銷售記錄)"` → `identify_destinations_nd4_mode()`
- `match()`: extend the single-piece guard from:
  ```python
  if mode == "ND限同OM轉貨(補0)" and transfer_qty == 1:
  ```
  to:
  ```python
  if mode in ("ND限同OM轉貨(補0)", "ND限同OM轉貨(補0及有銷售記錄)") and transfer_qty == 1:
  ```

### 4. `business_logic.py`
- **Docstring** (line 1, 64): add ND4 to mode list, bump version to v2.29.0
- **Import** `ND3_KEEP_STOCK` already imported (ND4 reuses it)
- **Line 364**: add `'_dests_nd4_mode'` to the strategy destination routing list:
  ```python
  if mode_def.strategy_key and mode_def.dest_method in ('_dests_f_mode', '_dests_e_mode', '_dests_b_special', '_dests_simplified_sku', '_dests_nd_mode', '_dests_nd3_mode', '_dests_nd4_mode'):
  ```
- **`generate_transfer_recommendations` docstring** (line 497): add ND4

### 5. `services/notes.py`
Import line 5: no change needed (`ND3_KEEP_STOCK` reused)

**Source note Section** (line 18): add ND4 source type:
```python
if src_type == 'ND4轉出(保留3件)':
    total_sales = source.get('last_month_sold_qty', 0) + source.get('mtd_sold_qty', 0)
    if total_sales == 0:
        return f"剩{remaining}件(ND4保留3件,0銷優先)"
    return f"剩{remaining}件(ND4保留3件,近2月銷{total_sales})"
```

**Dest note Section** (line 89): add ND4 dest type:
```python
if dest_type == 'ND4補0接收(有銷量)':
    target_qty = dest.get('target_qty', 0)
    return f"目標{target_qty}(S×0.5),已收{cumulative}"
```

### 6. `ui/sidebar.py`
- **Line 51 core functions**: update expander label from 28 to 29 模式
- **Line 63**: add `ND4模式(ND限同OM轉貨-補0及有銷售記錄)`
- **Line 103, 105**: update operation guide mode list to include ND4
- **Line 137**: ND4 is already in `get_receive_limit_codes()` (via ModeDef `receive_site_limit=True`)
- **Line 419**: add ND4 mode description section:
  ```
  **ND4模式(ND限同OM轉貨-補0及有銷售記錄)**
  - 繼承ND3全部規則（參考C1、同OM、轉出保留3件、零庫存ND補貨）
  - 接收端額外限制：僅限擁有銷售記錄的 Shop（Last Month Sold Qty + MTD Sold Qty > 0）
  - 無銷售記錄的零庫存 ND 店不收貨
  ```
- **Line 469 special conditions**: add ND4 entry:
  ```
  - ND4模式：繼承ND3 + 僅限有銷售記錄Shop接收, 無銷量零庫存ND不收
  ```

### 7. `ui/tutorial.py`
- **Line 19 group def**: `"nd_sku"` label from `ND1 / ND2 / ND3` to `ND1 / ND2 / ND3 / ND4`
- **Line 203 global rules**: update ND exception from `ND1/ND2/ND3` to `ND1/ND2/ND3/ND4`
- **Line 220 decision guide**: `"ND店舖互轉？"` from `"ND1 / ND2 / ND3"` to `"ND1 / ND2 / ND3 / ND4"`
- **Line 256**: add row `| ND店舖補零庫存+保留3件+僅限有銷售記錄Shop | ND4 |`

### 8. `data/tutorials/nd_sku.json`
Add ND4 tutorial entry (after ND3, before 精簡SKU). Key differences from ND3:
- `code`: `"ND4"`, `name`: `"ND限同OM轉貨(補0及有銷售記錄)"`
- Source flow: same as ND3
- Dest flow: add sales filter node text
- Scenario table: add a row showing HA006 has 0 stock but 0 sales → NOT receiving
- Diff table: compare ND3 vs ND4

### 9. `app.py`
- **Line 3 docstring**: update version to v2.29.0, add ND4 to mode list, change 28 → 29 模式
- **Line 5**: update 28 → 29 種

### 10. `README.md`
- **Line 1**: update version to v2.29.0
- **Line 7**: update 28 → 29 模式
- **Line 33**: add ND4 entry after ND3:
  ```markdown
  - **ND4：ND 限同 OM 轉貨（補 0 及有銷售記錄）（ND 同 OM 轉貨，轉出保留 3 件，只補有銷售記錄的零庫存 ND 店）**
  ```
- **Line 242**: add ND4 row to comparison table:
  ```markdown
  | ND4 | ND 限同OM轉貨(補0及有銷售記錄) | ND 同 OM 轉貨，轉出保留 3 件，只補有銷售記錄的零庫存 ND 店，按銷量排序，可限制單一出貨店配對接收店數 |
  ```
- **Line 251**: add ND4 to receive limit list
- **Line 269**: update to `ND1 / ND2 / ND3 / ND4`
- **Line 312**: add ND4 to receive limit list

### 11. `VERSION.md`
Add v2.29.0 entry (following the existing format):
```markdown
## v2.29.0 (2026-07-15)

### 新增 ND4 模式：ND 限同 OM 轉貨（補 0 及有銷售記錄）

#### 模式說明
- **ND4**：基於 ND3（ND限同OM轉貨補0），但對接收店舖增加銷售記錄限制
- 轉出規則與 ND3 完全相同（保留 3 件、同 OM、僅 ND 源）
- **接收端差異**：僅 Last Month Sold Qty + MTD Sold Qty > 0（有銷售記錄）的零庫存 ND 店舖才可接收
- 無銷售記錄的零庫存 ND 店舖不會收到任何貨品

#### 修改檔案
- `config.py` — 版本號 bump 至 v2.29.0
- `models/mode_registry.py` — MODE_DEFS 新增 ND4 ModeDef
- `strategies/nd_mode.py` — 新增 identify_sources_nd4_mode、identify_destinations_nd4_mode、NDModeStrategy 路由
- `business_logic.py` — dest_method 路由更新、docstring 版本更新
- `services/notes.py` — 新增 ND4 轉出/接收分析 note
- `ui/sidebar.py` — ND4 模式描述、功能清單、操作指引
- `ui/tutorial.py` — 教學內容更新（決策指南、全局規則、群組標題）
- `data/tutorials/nd_sku.json` — 新增 ND4 教學 JSON 資料
- `app.py` — docstring 版本更新（v2.29.0，29 模式）

#### 文件同步
- `README.md` — 模式列表、模式對照表、功能亮點、介面流程更新
- `VERSION.md` — 本版本記錄
- `config.py` — 版本號 bump 至 v2.29.0
- `app.py` — docstring 版本更新
- `ui/tutorial.py` — ND4 教學內容
- `data/tutorials/nd_sku.json` — ND4 教學資料
- `調貨模式詳解.txt` — 新增 ND4 詳細規則
- `transfer_logic_ai_brief.md` — 新增 ND4 條目
```

### 12. `調貨模式詳解.txt`
- Add ND4 summary table entry (after ND3 in `模式一覽表`)
- Add key differences comparison table entry (after ND3)
- Add full ND4 mode section (following ND3 at line 1325):
  - Core features: inherits ND3 + sales record filter
  - Source rules: identical to ND3
  - Receive rules: ND3 + `Last Month Sold Qty + MTD Sold Qty > 0`
  - Matching rules: same as ND3
  - Comparison: ND3 vs ND4

### 13. `transfer_logic_ai_brief.md`
- **Line 83 mode count**: 28 → 29
- **Line 370/397**: add ND4 to mentions
- Add full ND4 mode section (after ND3, following line 337):
  ```
  ## Mode ND4: ND Same-OM Zero-Fill with Sales Record Filter (補0及有銷售記錄)
  - Source rules: same as ND3
  - Destination rules:
    - ND stores with SaSa Net Stock == 0
    - ADDITIONAL: Last Month Sold Qty + MTD Sold Qty > 0 (sales record required)
    - Target Qty = max(Safety Stock * 0.5, 3)
    - Sort by 2-month sales descending
  - Same OM only
  - Uses nd_mode strategy
  ```

### 14. `data_processor.py`
- **Line 4 docstring**: update mode list to include ND4

### 15. `excel_generator.py`
- **Line 4 docstring**: update mode list to include ND4

---

## Validation

1. Run `python -m pytest tests/ -q` to verify no regressions
2. Verify ND4 appears in sidebar mode selection
3. Verify ND4 receive site limit radio appears
4. Verify ND4 tutorial content renders correctly
5. Verify ND4 source still retains 3 pieces like ND3
6. Verify ND4 destination requires sales > 0 (a store with net_stock=0 and sales=0 is skipped)
7. Verify ND4 destination accepts a store with net_stock=0 and sales>0

---

## Open Questions
- None — requirements are clear and ND3 provides a complete reference implementation.
