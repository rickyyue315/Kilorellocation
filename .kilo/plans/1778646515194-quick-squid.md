# business_logic.py 進一步優化分析

## 現狀（已實施 Phase 1-5）
- **business_logic.py**: 1537 行（已從 2247 行減少 31.6%）
- **已提取**: E2策略、Note服務、QC服務、Statistics服務、死代碼移除、模式列表精簡
- **剩餘結構**: 核心匹配引擎（`_sources_*`、`_dests_*`、`match_transfers`、`_match_by_priority`、`_compute_transfer_qty`、`_can_transfer`、`_optimize_single_piece_transfers`、`generate_transfer_recommendations`）

## 進一步優化空間評估

### 🟢 低風險、中等收益（建議實施）

#### P-A: `match_transfers` 重複調用 → 循環（-30 行）
- **現狀** (lines 946-979): 6 次幾乎相同的 `_match_by_priority` 調用，僅 `(source_priority, dest_priority, source_type_filter)` 不同
- **方案**: 用 tuple 列表 + 循環替代
```python
rounds = [
    (1, 1, None, None),       # ND→緊急缺貨
    (1, 2, None, None),       # ND→潛在缺貨
    (2, 1, 'RF過剩轉出', None), # RF過剩→緊急
    (2, 2, 'RF過剩轉出', None), # RF過剩→潛在
    (2, 1, 'RF加強轉出', None), # RF加強→緊急
    (2, 2, 'RF加強轉出', None), # RF加強→潛在
]
if mode == self.mode_c:
    rounds.append((2, 1, None, '重點補0'))
for src_p, dst_p, src_filter, dst_filter in rounds:
    self._match_by_priority(...)
```
- **風險**: 極低，純結構重組
- **影響**: 僅 `match_transfers` 方法

#### P-B: `_compute_transfer_qty` 去重複（-20 行）
- **現狀**: lines 986-994 與 1018-1023 完全相同的容量檢查模式重複兩次；lines 996-1003 與 1025-1031 相同的 D-family 避免1件邏輯重複兩次
- **方案**: 合併為單次執行，在函數尾部統一 cap
- **風險**: 低，邏輯等價

#### P-C: 精簡冗餘 docstring（-15 行）
- `_match_by_priority` (lines 1116-1134): 15 行 docstring，大部分與參數名自解釋
- `generate_transfer_recommendations` (lines 1377-1386): 5 行 docstring
- **風險**: 極低

### 🟡 中風險、較高收益（可選）

#### P-D: 提取 `_optimize_single_piece_transfers` + `_refresh_recommendation_fields`（-152 行）
- `_optimize_single_piece_transfers` (lines 1275-1374, ~100 行)
- `_refresh_recommendation_fields` (lines 1222-1273, ~52 行)
- **方案**: 移到 `services/post_processing.py`
- **挑戰**: `_refresh_recommendation_fields` 調用 `self._create_recommendation_note` 和 `self._infer_source_rp_type` → 需傳入回調
- **風險**: 中，需要確保回調鏈正確

### 🔴 高風險或低收益（不建議）

#### 不建議: 模式常量緊縮
- 24 個 `self.mode_xxx` 常量 (lines 116-139) 共 24 行
- 測試大量訪問 `logic.mode_a`、`logic.mode_b_special` 等，改動影響面大
- **收益低、風險高**

#### 不建議: `_sources_general` 拆分
- 該方法 (lines 412-624, 213 行) 是最大的單一方法
- 但其 A/C/B 模式的轉出量計算邏輯緊密耦合，拆分後反而增加閱讀難度
- **收益有限、風險中等**

---

## 總結

| 優化項 | 預估節省 | 風險 | 建議 |
|--------|---------|------|------|
| P-A: match_transfers 循環化 | -30 行 | 🟢 極低 | ✅ 建議 |
| P-B: _compute_transfer_qty 去重 | -20 行 | 🟢 低 | ✅ 建議 |
| P-C: 精簡 docstring | -15 行 | 🟢 極低 | ✅ 建議 |
| P-D: 提取後處理到服務 | -152 行 | 🟡 中 | ⚠️ 可選 |
| **合計（P-A+B+C）** | **-65 行** | **低** | **1537→1472** |
| **合計（全部）** | **-217 行** | **中** | **1537→1320** |

**結論**: 剩餘核心匹配引擎（~1000 行）結構已相對緊湊，進一步優化空間有限（-4%~-14%）。最大收益在 P-D（-152 行）但風險較高。P-A/B/C 合計僅 -65 行，屬於「錦上添花」。

**建議**: 若無性能或維護痛點，1537 行已是合理水平，可不再進一步拆分。
