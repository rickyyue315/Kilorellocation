# Phase 2 + Phase 5 執行計畫 — KiLo Reallocation v2.11.1

> **狀態**：✅ **已完成** (2026-05-12)
> **目標**：抽取常數與配置（Phase 2）+ UI 分層（Phase 5）
> **風險**：🟢 低（純抽取，不改變業務邏輯）
> **並行**：兩階段可完全並行，最後合併 refactor `app.py`
> 
> **Commit**: `f5f9c22` — pushed to `origin/main` (force update)
> **app.py**: `1201 → 266 lines` (—78%)

---

## 0. 前置確認

| 項目 | 狀態 |
|------|------|
| `pytest tests/ -v --tb=short` 通過 | 待確認 |
| `git status` 乾淨 (無未提交變更) | 待確認 |
| 無未提交的程式碼變更 | 待確認 |

---

## 1. 檔案建立順序（由無依賴到有依賴）

```
Step 1:  config.py                  (無依賴)
Step 2:  models/__init__.py         (無依賴)
         models/mode.py
Step 3:  data/stores.json           (無依賴)
Step 4:  static/styles.css          (無依賴，CSS 顏色直接硬編碼)
Step 5:  ui/__init__.py             (無依賴)
         ui/styles.py
         ui/mojibake.py
Step 6:  ui/sidebar.py              (依賴 config.py)
Step 7:  ui/display.py              (依賴 config.py)
```

## 2. 既有檔案修改順序

```
Step 8:  data_processor.py          (引入 config + stores.json)
Step 9:  business_logic.py          (引入 config 取代 magic numbers)
Step 10: app.py                     (重大重構：使用所有新模組)
```

---

## 3. Step-by-step 明細

### Step 1: 建立 `config.py`

**內容**：
- `VERSION = "v2.11.1"` — 取代 app.py/business_logic.py 中 5+ 處硬編碼字串
- `_is_zeabur_runtime()`, `_get_env_int()` — 從 app.py 搬移（L55-79）
- `IS_ZEABUR_RUNTIME`, `ZEABUR_RESULT_PREVIEW_LIMIT` — 模組級常數
- Magic numbers（全部來自 business_logic.py）：
  - `A_MODE_PERCENTAGE_CAP = 0.2`（L490）
  - `B_MODE_PERCENTAGE_CAP = 0.5`（L556）
  - `C_MODE_PERCENTAGE_CAP = 0.3`（L527）
  - `C_MODE_ABS_CAP = 3`（L528）
  - `A_MODE_MIN_TRANSFER = 2`（L490）
  - `C1_MODE_MIN_TRANSFER = 2`（L575）
  - `SAFETY_RECEIVE_MULTIPLIER = 2`（L821,891）
  - `MIN_RECEIVE_FLOOR = 3`（L821,891）
  - `F_TARGET_MULTIPLIER = 0.5`（L785,1028,1063）
  - `F_TARGET_FLOOR = 3`（L785,1028,1063）
  - `SIMPLIFIED_SKU_RECEIVE_MULTIPLIER = 2`（L177-178,627-628）
  - `ND_RECEIVE_MULTIPLIER = 2`（L702）
  - `OUTLIER_CAP = 100000`（data_processor.py L450-451）
  - `FILE_SIZE_LIMIT_BYTES = 50 * 1024 * 1024`（data_processor.py L341）
- 欄位定義（從 data_processor.py 搬移）：
  - `REQUIRED_COLUMNS`, `OPTIONAL_COLUMNS`, `INTEGER_COLUMNS`, `STRING_COLUMNS`
- `THEME` dict（從 app.py L206-226 搬移）
- `ZEABUR_ENV_KEYS` list

**副作用**：⚠️ `IS_ZEABUR_RUNTIME` / `ZEABUR_RESULT_PREVIEW_LIMIT` 改為從 config 匯入，不影響行為。

### Step 2: 建立 `models/__init__.py` + `models/mode.py`

**`models/__init__.py`**：空檔案 (package marker)

**`models/mode.py`**：
- `Mode(Enum)` — 24 個模式代碼列舉（A, B, B2, B2a, B2L, B2La, B3, B3a, B3L, B3La, C, C1, C2, D, D2, E1, E1b, E2, F, F2, ND1, ND2, SIMPLIFIED_SKU_SAME, SIMPLIFIED_SKU_CROSS）
- `MODE_NAME_MAP: Dict[str, str]` — 取代 app.py L924-949 的 `mode_name_map`
- `MODE_DESCRIPTIONS: Dict[str, str]` — 取代 app.py L501-526 的 `mode_descriptions`
- Helper predicates（從 business_logic.py L68-98 的 TransferLogic 方法改為純函式）：
  - `is_b_special_mode(mode_name: str) -> bool`
  - `is_b3_family_mode(mode_name: str) -> bool`
  - `is_b_tourist_no_source_mode(mode_name: str) -> bool`
  - `is_b_l_retain_mode(mode_name: str) -> bool`
  - `is_d_family_mode(mode_name: str) -> bool`
  - `is_nd_transfer_mode(mode_name: str) -> bool`
  - `is_simplified_sku_mode(mode_name: str) -> bool`
- `MODE_NAME_TO_CODE(mode_name: str) -> str` 反向查找

**風險**：`MODE_NAME_MAP` 的 Key 是模式代碼（"A", "B" 等），Value 是中文名稱。business_logic.py 的 instance attributes（`self.mode_a = "保守轉貨"`）是 中文名 → 中文名。app.py 的 `mode_name_map`（L924-949）是 代碼 → 中文名。**兩者不同**。建立共享 dict 時需注意這兩個映射同時存在，但此次只替換 app.py 的 `mode_name_map`，不改變 business_logic.py。

### Step 3: 建立 `data/stores.json`

- 將 `data_processor.py` L21-108 的 `DEFAULT_STORE_DATA` dict 匯出為 JSON
- 格式：`{"HA02": {"shop": "駱克", "regional": "HK", ...}, ...}`
- 注意 `.gitignore` 未排除 `data/` 目錄

### Step 4: 建立 `static/styles.css`

- 從 `app.py` L229-372 抽取全部 CSS
- 直接將所有 `{theme['xxx']}` 替換為對應的硬編碼顏色值（因為 THEME dict 是靜態常數）
- 共約 143 行純 CSS

### Step 5: 建立 `ui/__init__.py` + `ui/styles.py` + `ui/mojibake.py`

**`ui/styles.py`**：
- `load_css()` — 回傳 `static/styles.css` 內容（使用 `Path(__file__).parent.parent / 'static' / 'styles.css'`）
- `get_theme() -> dict` — 回傳 THEME dict（可選擇從 config 匯入）

**`ui/mojibake.py`**（Phase 3.6 提前至此處執行，因 app.py 重構必須）：
從 app.py L82-195 搬移：
- `fix_mojibake_text(value)`
- `fix_mojibake_value(value)`
- `patch_streamlit_text_rendering()`
- 模組級 mojibake patching 邏輯（L194-195）
- 保留 `DeltaGenerator` 的 try-import

### Step 6: 建立 `ui/sidebar.py`

從 app.py L374-704 抽取，包裝為 `render_sidebar() -> dict`：

**回傳值**：
```python
{
    'mode_code': str,           # "A", "B", "B2", 等
    'transfer_mode': str,       # 完整 radio 標籤
    'b_special_max_receive_sites_per_source': Optional[int],
    'f2_allow_hd_transfer': bool
}
```

**包含**：
- 系統資訊卡片（L377-382）
- 核心功能 expander（L384-420）
- 操作指引 expander（L424-448）
- 模式選擇 radio（L454-467）— 保持 `key='transfer_mode'` 維持 session_state
- 接收店數限制 radio（L476-486）
- F2 HD 選項 radio（L489-498）
- 模式描述 caption（L528）
- 詳細模式說明 expander（L530-701）
- 時間戳 footer（L704）
- `st.markdown("---")` 分隔線（L422, 450, 703）

### Step 7: 建立 `ui/display.py`

從 app.py L894-1180 抽取結果展示邏輯：

**函式**：
- `render_upload_requirements(mode_code: str)` — 從 app.py L716-818 抽取的欄位說明 expander
- `render_data_preview(df, processing_stats)` — 資料查覽（L894-905）
- `render_kpi_cards(statistics)` — KPI 指標（L996-1001）
- `render_results_table(recommendations, df, current_run_key)` — 結果表格（L1005-1092）
- `render_statistics(statistics)` — 詳細統計（L1094-1154）
- `render_download_button(excel_data, excel_filename, current_run_key)` — 下載按鈕（L1168-1180）

**注意**：
- `IS_ZEABUR_RUNTIME` / `ZEABUR_RESULT_PREVIEW_LIMIT` 從 `config` 匯入
- `_stock_lookup`、`cumulative_transfers`、`display_data` 等區域變數維持在函式內部
- 避免任何 `st.session_state` 直接操作（由 app.py 管理）

### Step 8: 修改 `data_processor.py`

**變更**：
1. 移除 `DEFAULT_STORE_DATA` dict（L21-108）
2. 加上 `import json` 和 `from pathlib import Path`
3. `get_store_default_info()` 改為從 `data/stores.json` 載入：
   ```python
   _store_data_cache = None
   def _load_store_data():
       global _store_data_cache
       if _store_data_cache is None:
           path = Path(__file__).parent / 'data' / 'stores.json'
           with open(path, 'r', encoding='utf-8') as f:
               _store_data_cache = json.load(f)
       return _store_data_cache
   ```
4. 可選：將 `required_columns`、`optional_columns`、`integer_columns`、`string_columns` 改為從 `config` 匯入

**風險**：🟢 低。行為完全相同，僅儲存方式變更。

### Step 9: 修改 `business_logic.py`

**變更**：
1. 加上 `from config import (A_MODE_PERCENTAGE_CAP, B_MODE_PERCENTAGE_CAP, C_MODE_PERCENTAGE_CAP, ...)`
2. 逐項取代 magic numbers：
   - L490: `max(int(total_available * 0.2), 2)` → `max(int(total_available * A_MODE_PERCENTAGE_CAP), A_MODE_MIN_TRANSFER)`
   - L527: `int(total_available * 0.3)` → `int(total_available * C_MODE_PERCENTAGE_CAP)`
   - L528: `3` → `C_MODE_ABS_CAP`
   - L556: `max(int(total_available * 0.5), 2)` → `max(int(total_available * B_MODE_PERCENTAGE_CAP), B_MODE_MIN_TRANSFER)`
   - L821: `max(int(safety_stock * 2), 3)` → `max(int(safety_stock * SAFETY_RECEIVE_MULTIPLIER), MIN_RECEIVE_FLOOR)`
   - 依此類推（共約 15 處替換）

**注意**：
- 不對 `business_logic.py` 做任何結構性變更（這是 Phase 4 的工作）
- 純機械替換，改完後 `pytest tests/` 必須全部通過
- `DEFAULT_STORE_DATA` 移除不影響 business_logic.py（它不直接使用該 dict）

### Step 10: 修改 `app.py` — 重大精簡（目標 < 300 行）

**保留在 app.py 的內容（不可移出）**：
- Streamlit 頁面配置（L197-203） — `st.set_page_config`
- 模組級 Zeabur/mojibake side effects（L194-195）
- 主流程：upload → preprocess → analyze → display
- `_cached_preprocess`（L30-48）— 有 `@st.cache_data`
- `_parse_target_for_ui`（L163-173）— 被 `_find_f_mode_nd_target_conflicts` 使用
- `_find_f_mode_nd_target_conflicts`（L176-189）— 被 F/F2 模式使用
- progress_bar 管理（L827, 833, 846, 921, 973, 1182）
- session_state 管理（L914-918, 968-971, 1161-1166）

**改為 import 的項目**：
| 原始位置 | 新來源 |
|---------|--------|
| `_is_zeabur_runtime`, `IS_ZEABUR_RUNTIME`, `ZEABUR_RESULT_PREVIEW_LIMIT`, `_get_env_int` (L55-79) | `config` |
| `THEME` dict (L206-226) | `config` |
| CSS st.markdown (L229-372) | `ui/styles.load_css()` |
| 全部 sidebar 內容 (L374-704) | `ui/sidebar.render_sidebar()` |
| `mode_descriptions` (L501-526) | `models/mode.MODE_DESCRIPTIONS` |
| `mode_name_map` (L924-949) | `models/mode.MODE_NAME_MAP` |
| `_fix_mojibake_text/value`, `_patch_streamlit_text_rendering` (L82-195) | `ui/mojibake` |
| 欄位說明 expander (L716-818) | `ui/display.render_upload_requirements()` |
| 資料查覽 (L894-905) | `ui/display.render_data_preview()` |
| KPI 指標 (L996-1001) | `ui/display.render_kpi_cards()` |
| 結果表格 (L1005-1092) | `ui/display.render_results_table()` |
| 統計圖表 (L1094-1154) | `ui/display.render_statistics()` |
| 下載按鈕 (L1168-1180) | `ui/display.render_download_button()` |

**預估最終行數**：~250-350 行（原 1201 行）

---

## 4. 風險矩陣

| 步驟 | 風險 | 緩解 |
|------|------|------|
| Step 8: stores.json 路徑 | 🟡 `Path(__file__).parent / 'data' / 'stores.json'` 在 Zeabur/Streamlit Cloud 須正確 | 使用 `__file__` 相對路徑，確保 `data/` 目錄有 `__init__.py` 或加入 git |
| Step 9: magic number 替換遺漏 | 🟢 可透過 `git diff` 檢查無剩餘常數 | 全面搜尋 `0\.2`、`0\.3`、`0\.5`、`* 2`、`* 0\.5` |
| Step 10: session_state key 衝突 | 🟡 搬移 sidebar 後 `st.radio` 的 `key` 可能重複 | 所有 key 使用字串常數，與原值一致 |
| Step 10: 模組級 side effect 順序 | 🟡 mojibake patch 和 Zeabur detection 在 import 時執行 | mojibake 保留在 app.py 頂層；IS_ZEABUR_RUNTIME 從 config import 時已評估 |

---

## 5. 驗證清單

```bash
# 每個步驟完成後執行
python -c "from config import VERSION; print(VERSION)"  # 確認 import 正常
python -c "from models.mode import MODE_NAME_MAP; print(len(MODE_NAME_MAP))"  # 確認 24 模式
python -c "from ui.styles import load_css; css = load_css(); print(len(css))"  # 確認 CSS 載入

# Step 8 之後
python -c "from data_processor import DataProcessor; dp = DataProcessor(); info = dp.get_store_default_info('HA02'); print(info['shop'])"  # 確認 stores.json 載入

# Step 9 之後
pytest tests/ -v --tb=short  # 所有測試必須通過

# Step 10 之後
streamlit run app.py  # 手動驗證 UI
```

---

## 6. 總執行順序

```
Step 1:  config.py
    ↓
Steps 2-5: models/, data/, static/, ui/  （可並行建立，無依賴）
    ↓
Step 6-7: ui/sidebar.py, ui/display.py
    ↓
Step 8-9: data_processor.py, business_logic.py  （可並行修改）
    ↓
Step 10: app.py （最終整合）
    ↓
驗證: pytest + streamlit run
```

---

## 7. 注意事項

1. **不改變業務邏輯**：Phase 2 + 5 純粹是抽取和搬移，不修改任何演算法。
2. **測試覆蓋**：tests/ 只 import business_logic.py, data_processor.py, excel_generator.py，不受 app.py/UI 重構影響。
3. **`_parse_target_for_ui`**：保留在 app.py，雖然是 UI 輔助函式但被 F/F2 conflict check 使用。未來可移入 `ui/` 但非本次範圍。
4. **`_find_f_mode_nd_target_conflicts`**：保留在 app.py，是 UI 前置檢查。未來可移入 `core/` 但非本次範圍。
5. **版本追蹤**：確保 `data/stores.json`、`static/styles.css` 不排除在 `.gitignore` 之外。
