---
description: Push all changes to GitHub (origin: https://github.com/rickyyue315/Kilorellocation)
---
Push all current changes to the GitHub remote `origin` at https://github.com/rickyyue315/Kilorellocation.

Steps:
1. Run `git status` to see the current state.
2. Stage all changes with `git add -A`.
3. Create a commit with a concise, descriptive message based on the changes (follow existing commit style).
4. Push to origin with `git push origin HEAD`.
5. If no remote named `origin` exists, add it: `git remote add origin https://github.com/rickyyue315/Kilorellocation.git` then push.
6. Confirm the push was successful.

$ARGUMENTS
